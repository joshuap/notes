{
  "dependencies": {
    "node-watch": "^0.6.4",
    "puppeteer": "^5.3.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2",
    "unzipper": "^0.10.11"
  },
  "devDependencies": {
    "@types/puppeteer": "^3.0.2",
    "@types/unzipper": "^0.10.3"
  },
  "scripts": {
    "fetch-roam": "npx ts-node scripts/fetch-roam.ts",
    "build": "./bin/middleman build",
    "build:ci": "npm run fetch-roam && ./bin/middleman build --verbose"
  }
}
